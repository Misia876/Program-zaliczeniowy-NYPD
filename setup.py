from setuptools import setup, find_packages

setup(
    name="program_zaliczeniowy",
    version="0.1",   
    packages=find_packages(),
    install_requires=[
        "numpy"
        "pandas",]
  python_requires=">=3.9")
